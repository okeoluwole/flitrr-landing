# dependencies
/node_modules
/.pnp
.pnp.js

# the Remotion video project (its own branch): the regenerable weight
# must never ride a git add -A on another branch. GitHub rejects the
# webpack cache and headless-chrome binaries outright.
/video/node_modules/
/video/.remotion/
/video/out/

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code: ignore local agent state, but track shared skills
.claude/*
!.claude/skills/
